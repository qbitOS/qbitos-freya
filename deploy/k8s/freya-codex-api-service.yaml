# beyondBINARY quantum-prefixed | uvspeed | {n, +1, -n, +0, 0, -1, +n, +2, -0, +3, 1}
apiVersion: v1
kind: Service
metadata:
  name: freya-codex-api
  labels:
    app: freya-codex-api
spec:
  selector:
    app: freya-codex-api
  ports:
  - name: http
    port: 8787
    targetPort: 8787
  type: ClusterIP
